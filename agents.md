## Strict Rules for AI Agents
- Three developers are collaborating in this repository: Anurag Shre (Module 1 - Backend & Admin), Jaya Patel (Module 2 - Student CBT UI), and Anubhav Rai (Module 3 - Security & Anti-Cheating).
- Refer to `Team Instructions.md` for role boundaries and folder ownership.
- Review `project_overview.md`, `api.md`, and `README.md` to understand the architecture, polymorphic APIs, and cloud deployment settings before modifying files.
- The project is configured for cloud deployment on **Render** (Backend API in `server/`), **Vercel** (Student Frontend in `frontend/` and Admin Portal in `admin/`), and **MongoDB Atlas** (Database in `geobucket-cbt`).